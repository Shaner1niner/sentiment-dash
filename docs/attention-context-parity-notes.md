# Attention context parity notes

Planned module behavior:

- Attention Off removes chart-level attention context and marks.
- Attention Context keeps attention in hover/context only.
- Overlay Marks shows sparse attention spike markers anchored to price candles.
- Spike marker tooltips should include TF-IDF keywords when row-level keyword data is available.
